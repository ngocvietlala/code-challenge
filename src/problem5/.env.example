# Server
PORT=3000

# Database — SQLite file path, relative to the repo root
DB_STORAGE=data/problem5.sqlite

# Rate limiting — window in ms, max requests per window per IP
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_GLOBAL_MAX=100
RATE_LIMIT_WRITE_MAX=10
